# Backend Configuration - PRODUÇÃO
# Copie este arquivo para .env.prod e ajuste os valores conforme necessário

# Porta INTERNA do container (onde o app escuta)
PORT=8080

# Database Configuration (PostgreSQL)
POSTGRES_USER=conquista
POSTGRES_PASSWORD=conquista123
POSTGRES_DB=conquista_ai
POSTGRES_HOST=postgres
# Porta EXTERNA do PostgreSQL (mapeamento Docker)
POSTGRES_PORT=5434
# IMPORTANTE: Use porta 5432 (interna) no DATABASE_URL para comunicação entre containers
DATABASE_URL=postgres://conquista:conquista123@postgres:5432/conquista_ai?sslmode=disable

# Spellbook API
SPELLBOOK_API_URL=https://spellbook-api.klapowsko.com

# Porta EXTERNA do backend (mapeamento Docker)
BACKEND_PORT=8083

# CORS - Deixe vazio para usar defaults, ou configure origens separadas por vírgula
# Exemplo: CORS_ALLOWED_ORIGINS=https://conquista-ai.klapowsko.com,https://app.conquista-ai.klapowsko.com
CORS_ALLOWED_ORIGINS=
